en:
  site_settings:
    categories:
      jtech: Jtech
      jtech_dislike: Jtech — Dislike
      jtech_smtp: Jtech — Alternate SMTP
      jtech_mini_mod: Jtech — Mini-mod
      jtech_mod: Jtech — Mod
      jtech_dumbcourse: Jtech — Dumbcourse
      jtech_smart_search: Jtech — Smart search
      jtech_popup_notifications: Jtech — Desktop pop-ups
      jtech_disteleplus: Jtech — Disteleplus
      jtech_mod_whispers: "Jtech — Mod: Whispers"
      jtech_mod_notes: "Jtech — Mod: Notes"
      jtech_mod_notifications: "Jtech — Mod: Staff notifications"
      jtech_mod_topic_tools: "Jtech — Mod: Topic tools"
      jtech_mod_checklists: "Jtech — Mod: Checklists"

    # ── Jtech (master) ─────────────────────────────────────────────────────
    jtech_enabled: Enable the Jtech plugin bundle
    jtech_enabled_description: Master gate. Every sub-plugin still has its own
      enable setting below for fine-grained control.

    # ── Jtech — Dislike ────────────────────────────────────────────────────
    discourse_no_likes_enabled: Enable Dislike (phantom reactions)
    discourse_no_likes_enabled_description: Master switch for the Dislike
      sub-plugin. When off, like behavior is unchanged anywhere on the forum.

    no_reactions_category_ids: Phantom-reaction categories
    no_reactions_category_ids_description: Categories where likes/reactions
      are treated as phantom — visible in UI but excluded from stats.

    dislike_hide_like_button: Hide the like button entirely
    dislike_hide_like_button_description: When on, the like/reaction button
      does not render at all in restricted categories, and the backend
      rejects any like API call there.

    dislike_allowed_like_groups: Restrict liking to these groups
    dislike_allowed_like_groups_description: Only members of these groups can
      like in restricted categories. Leave empty to allow everyone.

    dislike_show_in_history: Show phantom likes in user history
    dislike_show_in_history_description: When on, phantom likes appear in
      the user's activity feed even though they don't count in stats.

    dislike_count_in_leaderboard: Count phantom likes in user stats
    dislike_count_in_leaderboard_description: When on, phantom likes
      increment likes_given / likes_received counters used by leaderboards.

    dislike_notification_cleanup_window_seconds: Liked-notification cleanup window (seconds)
    dislike_notification_cleanup_window_seconds_description: How far back the
      phantom-like cleanup looks when retro-deleting the "liked" notification.
      Raise it if Sidekiq backlogs let notifications slip through.

    dislike_record_audit_trail: Record audit trail of phantom likes
    dislike_record_audit_trail_description: When on, every phantom like is
      recorded in the discourse_no_likes_phantoms table for retroactive
      reporting. Turn off if you don't need the audit trail and want to
      avoid the extra row writes.

    purge_phantom_likes_now: Run a retroactive phantom-like purge
    purge_phantom_likes_now_description: Toggle on to clear phantom likes
      across the forum and recalculate stats for affected users. Resets
      itself off when the job finishes.

    # ── Jtech — Alternate SMTP ─────────────────────────────────────────────
    discourse_another_email_enabled: Enable alternate SMTP
    discourse_another_email_enabled_description: When on, outgoing emails
      are routed through the alternate SMTP server configured below
      instead of Discourse's default.

    discourse_another_email_smtp_address: SMTP server address
    discourse_another_email_smtp_port: SMTP server port
    discourse_another_email_smtp_authentication_mode: SMTP authentication mode
    discourse_another_email_smtp_username: SMTP username
    discourse_another_email_smtp_password: SMTP password

    discourse_another_email_force_from_domain: Force From-address domain
    discourse_another_email_force_from_domain_description: Rewrites the
      domain of every outgoing From address to this value (e.g.
      `example.com`). Leave empty to disable rewriting.

    discourse_another_email_force_smtp_username_to_sender: Force SMTP
      username to match sender
    discourse_another_email_force_smtp_username_to_sender_description: When
      on, the SMTP AUTH username is rewritten per-message to match the
      sender's local-part + From-address domain. Plus-addressing is
      stripped. Useful for SMTP relays that require AUTH user to match
      envelope-from.

    # ── Jtech — Mini-mod ───────────────────────────────────────────────────
    mini_mod_enabled: Enable Mini-mod
    mini_mod_enabled_description: Lets category group moderators create
      and edit categories they moderate, plus optional extras below.

    mini_mod_can_create_categories: Mini-mods can create categories
    mini_mod_can_create_categories_description: Category group moderators may
      create categories (subcategories under ones they moderate, or anywhere
      with "manage all categories").

    mini_mod_can_edit_categories: Mini-mods can edit categories
    mini_mod_can_edit_categories_description: Category group moderators may
      edit the categories they moderate (or all, with "manage all
      categories").

    mini_mod_can_edit_topics: Mini-mods can edit any topic
    mini_mod_can_edit_topics_description: With "manage all categories" on,
      category group moderators may edit any topic they can see, unless its
      first post is locked.

    mini_mod_can_move_topics: Mini-mods can move topics between categories
    mini_mod_can_move_topics_description: Category group moderators may move
      topics into categories they moderate (or any they can see, with
      "manage all categories").

    mini_mod_preload_admin_bundle: Preload the admin UI for mini-mods
    mini_mod_preload_admin_bundle_description: Preload the admin JavaScript
      bundle for non-staff category group moderators so admin-only category
      screens render for them. Grants no server-side rights by itself.

    mini_mod_manage_all_categories: Manage all categories
    mini_mod_manage_all_categories_description: When on, category group
      moderators can manage every category, not just the ones they
      moderate.

    mini_mod_manage_tags: Manage tags
    mini_mod_manage_tags_description: When on, category group moderators
      can create, edit, and delete tags.

    mini_mod_can_post_in_closed_topics: Post in closed topics
    mini_mod_can_post_in_closed_topics_description: When on, category
      group moderators can reply on closed topics in categories they
      moderate. Site staff are unaffected.

    mini_mod_can_reopen_topics: Reopen closed topics
    mini_mod_can_reopen_topics_description: When on, category group
      moderators can reopen closed topics in categories they moderate.
      Site staff are unaffected.

    # ── Trust Level 4 (Discourse core) ─────────────────────────────────────
    tl4_can_post_in_closed_topics: TL4 — post in closed topics
    tl4_can_post_in_closed_topics_description: When on, trust level 4
      users can reply on closed topics. Site staff are unaffected.

    tl4_can_reopen_topics: TL4 — reopen closed topics
    tl4_can_reopen_topics_description: When on, trust level 4 users
      can reopen closed topics. Site staff are unaffected.

    # ── Jtech — Mod ────────────────────────────────────────────────────────
    mod_categories_enabled: Enable Mod (moderator topic tools)
    mod_categories_enabled_description: Master switch for the moderator
      topic-tools sub-plugin (pre-checks, footers, reply prompts, whispers,
      moderator notes, checklists). Each feature has its own toggle below.

    mod_moderators_can_create_categories: Moderators can create categories
    mod_moderators_can_create_categories_description: Any moderator may create
      any category, site-wide.

    mod_moderators_can_edit_categories: Moderators can edit categories
    mod_moderators_can_edit_categories_description: Any moderator may edit any
      category, including its security settings.

    mod_moderators_can_delete_categories: Moderators can delete categories
    mod_moderators_can_delete_categories_description: Any moderator may delete
      an empty, childless category.

    precheck_new_topic_enabled: Per-category new-topic confirmation
    precheck_new_topic_enabled_description: When on, users see a 'before
      you post a new topic' prompt configurable per category.

    topic_footer_message_enabled: Per-topic footer message
    topic_footer_message_enabled_description: When on, moderators can pin
      a footer message on a topic that appears below all replies.

    topic_reply_prompt_enabled: Per-topic reply confirmation
    topic_reply_prompt_enabled_description: When on, users see a 'before
      you post a reply' prompt configurable per topic.

    mod_whisper_enabled: Moderator whispers
    mod_whisper_enabled_description: When on, staff can post in-topic
      whispers visible only to staff, the post author, the topic's whisper
      participants, and any users staff explicitly target.

    mod_whisper_add_participant_enabled: Whispers — add participants
    mod_whisper_add_participant_enabled_description: Staff may add a user to a
      topic's whisper conversation; that user then sees every past and future
      whisper in the topic.

    mod_whisper_convert_enabled: Whispers — convert posts
    mod_whisper_convert_enabled_description: Staff may toggle whisper status
      on an existing post (whisper to public and back) and retarget its
      audience.

    mod_whisper_badge_targeting_enabled: Whispers — badge targeting
    mod_whisper_badge_targeting_enabled_description: Whispers may target badge
      holders (membership evaluated lazily at read time) in addition to users
      and groups.

    mod_topic_private_notes_enabled: Private staff notes on topics
    mod_topic_private_notes_enabled_description: Staff may attach a private
      note (with a reply thread) to any topic, visible only to staff.

    mod_moderators_can_edit_others_notes: Moderators can edit others' notes
    mod_moderators_can_edit_others_notes_description: When on, any moderator
      may edit or delete notes and note replies written by other staff.
      Authors and admins always can.

    mod_note_view_tracking_enabled: Track who viewed a note
    mod_note_view_tracking_enabled_description: Record and show which staff
      members opened a topic's note panel, and when ("Viewed by N").

    mod_notes_feed_enabled: Moderator notes feed (shield tab)
    mod_notes_feed_enabled_description: The staff user-menu shield tab with
      the cross-forum feed of noted topics and staff events, plus its unread
      counter.

    mod_notify_staff_on_topic_notes: Notify staff on topic notes
    mod_notify_staff_on_topic_notes_description: When a staff member writes or
      replies to a topic note, notify every other admin and moderator.

    mod_auto_mark_notifications_seen: Auto-mark plugin notifications read
    mod_auto_mark_notifications_seen_description: Opening a topic or the
      review queue marks the plugin's own note/whisper notifications for it
      as read.

    mod_notification_type_filter_enabled: Notification type filter
    mod_notification_type_filter_enabled_description: Adds a type filter
      dropdown to the user notifications page (staff also get the mod-notes
      stream filter).

    mod_pin_post_enabled: Pin a post to the topic footer
    mod_pin_post_enabled_description: Moderators may pin any post to the
      bottom of its topic; the pinned copy and in-stream badge render for all
      viewers.

    mod_topic_require_reply_approval_enabled: Per-topic reply approval
    mod_topic_require_reply_approval_enabled_description: Moderators may flag
      a topic so all replies route to the review queue.

    mod_pm_badge_group_enabled: PM badge groups
    mod_pm_badge_group_enabled_description: Staff composing a personal
      message may splice in every holder of a badge as recipients. The badge
      member lookup is staff-only.

    mod_first_post_checklist_enabled: Forum-wide first-post checklist
    mod_first_post_checklist_enabled_description: Moderators may maintain a
      checklist every non-staff user (under the trust-level cap) must accept
      before posting, including the acceptance audit log and forced
      re-acceptance.

    mod_targeted_checklists_enabled: Targeted checklists
    mod_targeted_checklists_enabled_description: Moderators may aim a
      checklist at named users, gating their posting regardless of trust
      level. Admins are always exempt.

    mod_topic_prompt_checklist_enabled: Per-topic prompt checklists
    mod_topic_prompt_checklist_enabled_description: Moderators may attach a
      blocking checklist or statement to a topic, shown before posting in it.

    mod_notify_staff_on_post_actions: Notify staff on post actions
    mod_notify_staff_on_post_actions_description: When on, every other
      staff member gets a bell notification + live alert when a moderator
      deletes a post or rejects a queued post. Queued-post APPROVALS have
      their own opt-in setting below (default off) since approvals are
      routine and noisy.
    mod_notify_staff_on_post_approved: Notify staff on queued-post approvals
    mod_notify_staff_on_post_approved_description: When on, every other
      staff member gets a bell notification + live alert when a moderator
      approves a queued post. Off by default — approvals happen often and
      most staff don't need a notification for each one. Independent from
      the "post actions" setting above.
    mod_notify_staff_on_user_notes: Notify staff on user notes
    mod_notify_staff_on_user_notes_description: When on, every other staff
      member gets a bell notification + live alert when a moderator adds
      a note to a user's profile (discourse-user-notes).
    mod_notify_staff_on_flag_notes: Notify staff on flag notes
    mod_notify_staff_on_flag_notes_description: When on, every other staff
      member gets a bell notification + live alert when a moderator adds
      a note to a flag/reviewable in the review queue.

    # ── Jtech — Smart search ─────────────────────────────────────────────
    smart_search_enabled: Enable smart search (synonym expansion)
    smart_search_enabled_description: When on, searches are expanded with
      synonyms from a built-in dictionary so that, e.g., "kid" also finds
      posts about "child" or "youth". The original query runs first; only
      when it returns fewer than the result threshold below do synonym
      variants run and get merged in. All synonym work is in-process —
      no external services, no embedding models, no API keys. On any
      error the request silently falls back to vanilla Discourse search.
    smart_search_minimum_results: Smart search — minimum result threshold
    smart_search_minimum_results_description: Synonym-expanded variants
      only run when the original query returns fewer than this many
      posts. Set to 0 to always run variants (more thorough, more SQL).
    smart_search_variant_limit: Smart search — variant query cap
    smart_search_variant_limit_description: Hard cap on the number of
      synonym-expanded variants run per search. Each variant is one
      extra SQL query; raising this gives broader matches but slower
      search. Range 1–5.

    # ── Jtech — Desktop pop-ups ────────────────────────────────────────────
    popup_notifications_enabled: Enable desktop pop-up notifications
    popup_notifications_enabled_description: Master switch for the Jelly-style
      pop-up notification card (top-right, desktop only). When off, no card
      appears for anyone and the per-user preference is hidden. This is purely
      additive — it never changes the bell counter, the notifications dropdown,
      or read state.
    popup_notifications_default_enabled: Pop-ups on by default
    popup_notifications_default_enabled_description: The default for users who
      have not chosen on their account page. Off by design, so enabling the
      plugin never surprises the whole forum — each user opts in themselves.
    popup_notifications_max_stack: Pop-up stack depth
    popup_notifications_max_stack_description: How many toast cards may be on
      screen at once; a further notification drops the oldest.

    popup_notifications_timeout_seconds: Pop-up auto-dismiss (seconds)
    popup_notifications_timeout_seconds_description: How long the card stays on
      screen before dismissing itself. Clicking anywhere else also dismisses it.

    # ── Jtech — Dumbcourse ─────────────────────────────────────────────────
    dumbcourse_enabled: Enable Dumbcourse
    dumbcourse_enabled_description: Serves the Dumbcourse SPA under the
      configured base path. Other Dumbcourse features below still respect
      their own toggles when this is on.

    dumbcourse_base_path: Base path
    dumbcourse_base_path_description: URL prefix the SPA mounts at
      (default `dumb` → serves at /dumb).

    dumbcourse_default_theme: Default theme
    dumbcourse_default_view: Default view

    dumbcourse_sidebar_link_enabled: Show in sidebar
    dumbcourse_sidebar_link_enabled_description: When on, a "Dumbcourse"
      link appears in the main Discourse sidebar.

    dumbcourse_show_category_names: Show category names on topic list
    dumbcourse_topic_posters_visibility: Latest poster avatars on topic list
    dumbcourse_online_glow_enabled: Online glow on user avatars

    dumbcourse_topic_posters_visibility_choices:
      all: All devices
      desktop: Desktop only
      mobile: Mobile only
      none: 'Off'

    dumbcourse_pagination_enabled: Use pagination
    dumbcourse_pagination_enabled_description: When on, topic lists use
      page-based navigation instead of infinite scroll.

    dumbcourse_topics_per_page: Topics per page
    dumbcourse_topics_per_page_description: How many topics appear on each
      page when pagination is enabled.

    dumbcourse_push_enabled: Enable push notifications
    dumbcourse_push_enabled_description: When on, the SPA can register
      service workers for web push. Server-sent events are disabled; push
      is delivered via Redis publish.

    dumbcourse_languagetool_enabled: Enable LanguageTool Refine button
    dumbcourse_languagetool_enabled_description: When on, the composer
      gains a Refine (spell/grammar) button backed by LanguageTool.

    dumbcourse_languagetool_mode: LanguageTool mode
    dumbcourse_languagetool_mode_description: Choose between a self-hosted
      LanguageTool server and the official LanguageTool cloud API.

    dumbcourse_languagetool_mode_choices:
      self_hosted: Self-hosted
      official_api: Official API

    dumbcourse_languagetool_url: Self-hosted server URL
    dumbcourse_languagetool_url_description: Base URL of your self-hosted
      LanguageTool server (do NOT include /v2/check).

    dumbcourse_languagetool_secret: Self-hosted server secret
    dumbcourse_languagetool_secret_description: Optional bearer token sent
      as the Authorization header to the self-hosted server.

    dumbcourse_languagetool_api_url: Official API endpoint
    dumbcourse_languagetool_api_url_description: 'Official LanguageTool
      endpoint. Default: https://api.languagetool.org/v2/check.'

    dumbcourse_languagetool_api_username: Official API username
    dumbcourse_languagetool_api_username_description: Only required for
      official API plans that use username + API key authentication.

    dumbcourse_languagetool_api_key: Official API key
    dumbcourse_languagetool_api_key_description: Required when a username
      is set.

    # ── Jtech — Disteleplus ────────────────────────────────────────────────
    disteleplus_enabled: Enable Disteleplus (Telegram ⇄ chat bridge)
    disteleplus_enabled_description: Master switch for the two-way bridge
      between one Telegram group and one Discourse chat channel.

    disteleplus_bot_token: Telegram bot token
    disteleplus_bot_token_description: Token from @BotFather (/newbot). The
      bot must be in the bridged group, be a group admin, and have privacy
      mode DISABLED (/setprivacy) or it will not see group messages.

    disteleplus_webhook_secret: Webhook secret
    disteleplus_webhook_secret_description: Shared secret Telegram echoes in
      the X-Telegram-Bot-Api-Secret-Token header. Leave blank — the register
      webhook button generates one automatically.

    disteleplus_telegram_chat_id: Telegram group chat id
    disteleplus_telegram_chat_id_description: The bridged group's chat id (a
      negative number, e.g. -1001234567890). Find it via getUpdates or
      @RawDataBot after adding the bot to the group.

    disteleplus_chat_channel_id: Discourse chat channel id
    disteleplus_chat_channel_id_description: The bridged chat channel's id —
      the number in its URL, /chat/c/-/<id>.

    disteleplus_bridge_bot_username: Bridge bot username
    disteleplus_bridge_bot_username_description: Discourse user that posts
      unmatched Telegram messages. Created automatically (at trust level 4)
      if it doesn't exist; point it at an existing user to reuse one.

    disteleplus_user_mappings: Telegram → Discourse user mappings
    disteleplus_user_mappings_description: 'Manual pairs in the form
      telegram_username:discourse_username. Takes precedence over the
      automatic same-username match. Anyone controlling a mapped Telegram
      account posts as that Discourse user — map people you trust.'

    disteleplus_bridge_uploads: Bridge media/uploads
    disteleplus_bridge_uploads_description: Copy photos, files, video and
      audio across the bridge (both directions), up to the size limit below.

    disteleplus_max_upload_mb: Max bridged file size (MB)
    disteleplus_max_upload_mb_description: Media larger than this becomes a
      placeholder/link instead. Hard ceiling 20 MB — the Bot API refuses
      larger bot downloads.

    disteleplus_bridge_edits: Bridge message edits
    disteleplus_bridge_edits_description: Mirror edits both ways. Note that
      Discourse-side edits of Telegram-originated messages stay local — bots
      cannot edit other people's Telegram messages.

    disteleplus_bridge_deletes: Bridge deletions (Discourse → Telegram)
    disteleplus_bridge_deletes_description: Deleting a bridged chat message
      also deletes it in Telegram (bot needs delete rights; Telegram allows
      it for 48 hours). Telegram-side deletions are never reported to bots,
      so they cannot be mirrored back.

    disteleplus_bridge_polls: Bridge Telegram polls
    disteleplus_bridge_polls_description: Render Telegram polls as a markdown
      snapshot in chat and refresh vote counts as updates arrive. Voting from
      Discourse is not possible.

    disteleplus_bridge_reactions: Bridge reactions
    disteleplus_bridge_reactions_description: Telegram reactions appear as
      chat reactions (per user, when matched). Discourse reactions collapse
      to the bot's single allowed Telegram reaction — the most recent one.

    disteleplus_lock_chat_ui: Lock chat to the bridge channel
    disteleplus_lock_chat_ui_description: The chat button opens the bridge
      channel directly and creating channels or direct messages is hidden and
      blocked. Chat becomes this one group conversation.

    disteleplus_lock_chat_exempt_admins: Exempt admins from the chat lock
    disteleplus_lock_chat_exempt_admins_description: When on, admins keep the
      full chat UI even while the lock is active.

    disteleplus_register_webhook_now: Register the Telegram webhook now
    disteleplus_register_webhook_now_description: Flip on to call setWebhook
      (generating the secret first if blank). Resets itself once enqueued;
      check /logs if messages still don't arrive.

  discourse_mod_categories:
    note_notification_alert: '%{username} added a moderator note on "%{topic}"'
    note_reply_notification_alert: '%{username} replied to a moderator note on "%{topic}"'
    post_deleted_notification_alert: '%{username} deleted a post in "%{topic}"'
    post_approved_notification_alert: '%{username} approved a queued post in "%{topic}"'
    post_rejected_notification_alert: '%{username} rejected a queued post'
    user_note_notification_alert: '%{username} added a note on %{target}'
    flag_note_notification_alert: '%{username} added a note on a flag for %{target}'
